package com.example

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Basic unit test to ensure build suite configurations and Kotlin execution compile cleanly.
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
